// файл — файлын оролт гаралт
extern функц файл_унших_бүтэн(зам мөр) -> мөр {}
extern функц файл_бичих(зам мөр, агуулга мөр) -> тоо {}
// файл_бичих_байт — урт байтыг (NUL-ийг оруулаад) файлд бичнэ.
extern функц файл_бичих_байт(зам мөр, буф мөр, урт тоо64) -> тоо {}
