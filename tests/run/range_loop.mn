// regression: давт loop variable was never added to the symbol table
// шалгах: гаралт "15\n"
функц үндсэн() -> тоо {
    зарла с: тоо = 0;
    давт и бол 1..5 хүртэл {
        с = с + и;
    }
    хэвлэ(с);
    мөр_хэвлэх("\n");
    буц 0;
}
