services:
  playground:
    build:
      context: ..
      dockerfile: playground/Dockerfile
    ports:
      - "8080:8080"
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "1.0"
