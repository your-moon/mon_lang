// Range loop
функц үндсэн() -> тоо {
    зарла нийлбэр:тоо = 0;

    давт и бол 1..5 хүртэл {
        нийлбэр = нийлбэр + и;
    }

    // While loop
    зарла а:тоо = 0;
    давтах а < 5 бол {
        а = а + 1;
        хэвлэ(а);
    }

    буц нийлбэр; // 15 (1+2+3+4+5)
}
