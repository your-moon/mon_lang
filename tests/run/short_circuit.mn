// && and || must not evaluate the RHS once decided (RHS would SIGFPE)
// шалгах: гаралт "0 1\n"
функц үндсэн() -> тоо {
    зарла тэг: тоо = 0;
    зарла а: тоо = 0 && (1 / тэг);
    зарла б: тоо = 1 || (1 / тэг);
    хэвлэ(а);
    мөр_хэвлэх(" ");
    хэвлэ(б);
    мөр_хэвлэх("\n");
    буц 0;
}
