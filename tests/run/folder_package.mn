// folder package: `ашигла "dir"` pulls in every .mn file in that directory
// шалгах: гаралт "23\n"
ашигла "тоолуур_багц";

функц үндсэн() -> тоо {
    хэвлэ(нэм(үржүүл(4, 5), 3));   // 4*5 + 3 = 23
    мөр_хэвлэх("\n");
    буц 0;
}
