// Ternary conditional
функц үндсэн() -> тоо {
    зарла а:тоо;
    а = 1 == 1 ? 3 == 4 ? 4*5+3 : 4*4 : 3*3;

    // If/else
    хэрэв 1 == 1 бол {
        буц а; // 16
    } үгүй бол {
        буц 1;
    }
}
