// мөр — тэмдэгт мөр, байтын үйлдлүүд
extern функц мөр_урт(с мөр) -> тоо64 {}
extern функц байт(с мөр, и тоо64) -> тоо {}
extern функц байт_тавих(с мөр, и тоо64, б тоо) -> хоосон {}
extern функц мөр_шинэ(урт тоо64) -> мөр {}
