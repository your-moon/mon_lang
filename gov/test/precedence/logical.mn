функц үндсэн() -> тоо {
    зарла а:тоо = 5;
    зарла б:тоо = 10;
    зарла в:тоо = 15;

    // AND iig turuulj parse hiine
    хэрэв а < б && б < в || а > в бол {
        буц 1;
    }

    // haaltiig ehelj bodno
    хэрэв (а < б || б > в) && а < в бол {
        буц 2;
    }

    буц 0;
}
