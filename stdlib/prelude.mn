extern функц хэвлэ(н тоо64) -> хоосон {}
extern функц эхэвлэ(н этоо64) -> хоосон {}
extern функц мөр_хэвлэх(м мөр) -> хоосон {}
extern функц тэмдэгтХэвлэх(т тэмдэгт) -> хоосон {}
extern функц унш() -> тоо64 {}
extern функц унш32() -> тоо {}
extern функц санамсаргүйТоо(н тоо) -> тоо {}
extern функц одоо() -> тоо64 {}
extern функц чөлөөлөх(п тоо[]) -> хоосон {}
extern функц хүлээх(мс тоо) -> хоосон {}
extern функц дэлгэцЦэвэрлэх() -> хоосон {}
extern функц мөрУрт(с мөр) -> тоо64 {}
extern функц байт(с мөр, и тоо64) -> тоо {}
extern функц байтТавих(с мөр, и тоо64, б тоо) -> хоосон {}
extern функц файлУншихБүтэн(зам мөр) -> мөр {}
extern функц файлБичих(зам мөр, агуулга мөр) -> тоо {}
extern функц аргументТоо() -> тоо {}
extern функц аргумент(и тоо) -> мөр {}
extern функц мөрШинэ(урт тоо64) -> мөр {}
