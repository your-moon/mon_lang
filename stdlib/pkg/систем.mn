// систем — процесс, орчин, хугацаа
extern функц аргумент_тоо() -> тоо {}
extern функц аргумент(и тоо) -> мөр {}
extern функц одоо() -> тоо64 {}
extern функц хүлээх(мс тоо) -> хоосон {}
extern функц санамсаргүй_тоо(н тоо) -> тоо {}
extern функц дэлгэц_цэвэрлэх() -> хоосон {}
